import 'package:flutter/material.dart';

class AppDimens {
  static const marginHorizontal = 16.0;

  static const radiusLg = Radius.circular(20);
  static const radiusMd = Radius.circular(12);
  static const radiusSm = Radius.circular(8);

  static const borderRadiusLg = BorderRadius.all(radiusLg);
  static const borderRadiusMd = BorderRadius.all(radiusMd);
  static const borderRadiusSm = BorderRadius.all(radiusSm);
}
